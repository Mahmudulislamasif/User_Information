import React from 'react';

const User = ({user}) => {
    const {_id,name,image,description,minimum,available,price}=user;
   
    return (
        <div>
            
        </div>
    );
};

export default User;